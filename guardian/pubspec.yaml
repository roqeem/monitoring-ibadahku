name: monitoring_ibadahku
description: "Monitoring IbadahKu - Aplikasi orang tua memantau ibadah anak"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  intl: ^0.20.0
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.1
  firebase_analytics: ^11.3.1
  http: ^1.6.0
  google_sign_in: ^6.2.1
  fl_chart: ^0.69.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true